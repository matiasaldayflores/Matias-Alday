# Third-party notices

- `scripts/gcode_tool.py` and parts of `references/slicing.md` are vendored/adapted from [earthtojake/text-to-cad](https://github.com/earthtojake/text-to-cad) (MIT License — see LICENSE).
- The overall describe → generate → validate → iterate workflow is inspired by the same project's `cad` skill.
